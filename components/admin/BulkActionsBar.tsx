export default function BulkActionsBar() {
  // Implement bulk selection and actions in UsersTable
  return (
    <button onClick={() => alert('Bulk actions coming soon!')}>Bulk Actions</button>
  );
}
